const a = ['1', '2', '3', '4', '5', '6', '7'];

/*it transforms the array and shows it in a new array
further filter and every can be used to handle complecated problems
*/

const b=a.map(num=>num*2)
console.log(b)